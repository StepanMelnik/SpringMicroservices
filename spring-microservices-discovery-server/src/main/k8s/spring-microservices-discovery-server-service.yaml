apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring-microservices-discovery-server
  name: spring-microservices-discovery-server
  namespace: default
spec:
  selector:
    app: spring-microservices-discovery-server
  ports:
    - name: http
      port: 8761
      targetPort: http
      protocol: TCP
  type: LoadBalancer
