apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: spring-microservices-config-server
  name: spring-microservices-config-server
  namespace: default
spec:
  selector:
    matchLabels:
      app: spring-microservices-config-server
  replicas: 1
  template:
    metadata:
      labels:
        app: spring-microservices-config-server
    spec:
      containers:
        -
          image: sme/micro-config-server:0.1
          name: spring-microservices-config-server
          ports:
            -
              containerPort: 8888
              name: http
          env:
            - name: DISCOVERYSERVER_URI
              value: "http://discoveryserver:8761/eureka/"
            - name: DISCOVERYSERVER_PORT
              value: "8761"
          volumeMounts:
            - mountPath: /log/
              name: log4j    
          resources:
            requests:
              memory: "64Mi"
              cpu: "125m"
            limits:
              memory: "128Mi"
              cpu: "250m"
      restartPolicy: Always
      volumes:
        - name: log4j
          hostPath:
            # directory location on host
            path: /var/log/microservices
            type: Directory
      
      