spring:
  application:
    name: gatewayservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
  devtools:
    add-properties: true
# Debug
#debug: true
# Allows to trace all calls
#trace: true
